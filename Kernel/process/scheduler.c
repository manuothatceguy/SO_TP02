#include <scheduler.h>

uint64_t schedule(uint64_t rsp){
    // por ahora no hace nada entonces el próximo proceso es el mismo
    // en el futuro se va a tener que elegir el próximo proceso
    // y devolver su rsp
    return rsp;
}